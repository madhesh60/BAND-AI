# Experiments

This folder contains exploratory scripts and quick experiments used during development.

| File | Purpose |
|------|---------|
| `test_pickle_saver.py` | Manual test for the `PersistentMemorySaver` pickle-based LangGraph checkpointer |

These are **not** part of the production pipeline. Run them individually with `uv run python experiments/<file>.py`.
